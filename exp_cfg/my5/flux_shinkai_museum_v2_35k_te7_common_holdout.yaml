# 只用于公平评估 `35k te7` 的共同 holdout 配置。
# benchmark 与 `35k base` 共用同一份 partition, 保证对比口径一致。

base_dir: outputs/eval_tmp/my5_colmap_fastgs_stable_35k_dense_te7_common_holdout
exp_name: flux_shinkai_museum_v2_common_holdout

refine_start_idx: 0
refine_end_idx: 6

train_start_idx: 0
train_end_idx: 6

test_split: test
test_trans: [0, 0, 0]
test_rots: [0, 0, 0]

prompt: "high detail. Refine images, repair visual errors and blurriness in AI-generated images, perform defogging and enhance clarity. Correct unreasonable elements, data errors and illogical content in the images, remove ghosting, and boost details, textures and edges."

strength: 0.65
refine_steps: 400
warp_ratio: 0.3

hessian_attr: ["means", "quats", "scales"]

data_type: colmap
load_step: 34999

flux_model_path: /home/rais/.cache/modelscope/hub/models/black-forest-labs/FLUX___1-dev
