# 任务计划: 评估 `flux_shinkai_museum_v2_pose_jitter_train_test_x3_20260330` 的 NSPR

## [2026-04-01 07:01:19] [Session ID: 4138] [记录类型]: 初始化评估计划

## 目标

查清 `flux_shinkai_museum_v2_pose_jitter_train_test_x3_20260330` 对应实验的 `NSPR` 到底是多少, 并明确这个数字来自哪份产物、按什么口径得到。

## 阶段

- [x] 阶段1: 回读项目经验与主线/支线历史上下文
- [ ] 阶段2: 定位实验目录、评估脚本和 `NSPR` 定义
- [ ] 阶段3: 提取或重算目标实验的 `NSPR`
- [ ] 阶段4: 复核证据并整理交付结论

## 关键问题

1. `NSPR` 在当前项目里是不是一个明确存在的指标名: 还没确认。目前只定位到了实验目录和历史记录, 但仓库全文搜索还没有找到清晰的 `NSPR` 指标定义。
2. 用户给的实验名是否能在仓库里找到真实对象: 能。已经定位到配置文件 `exp_cfg/my5/flux_shinkai_museum_v2_35k_pose_jitter_train_test_x3_20260330.yaml` 和对应输出目录历史记录。
3. 当前应该优先走哪条评估路径:
   - 方案A(最佳方案): 先查明项目内 `NSPR` 的真实定义, 再读取已有评估产物或按同口径重算。
   - 方案B(先能用方案): 如果项目里没有显式 `NSPR` 定义, 则结合最接近的现有评估日志判断用户是否实际想问 `PSNR` 或其他指标, 并明确标注这是口径推断而不是已验证结论。

## 做出的决定

- 决定1: 先不把 `NSPR` 自动当成 `PSNR` 或其它常见图像指标, 避免在指标名上先入为主。
- 决定2: 先用已有实验输出和历史支线记录建立事实基线, 再决定是否需要重新跑评估。
- 决定3: 这轮输出必须按“现象 -> 假设 -> 验证计划 -> 结论”的口径组织, 没有动态证据就不直接报数。

## 遇到错误

- 暂无新错误, 当前阻塞点是 `NSPR` 指标定义尚未确认。

## 状态

**目前在阶段2** - 正在定位 `NSPR` 的定义、相关评估脚本和目标实验现有评估产物。

## [2026-04-01 07:08:15] [Session ID: 4138] [记录类型]: 评估完成并确认应以 `PSNR` 口径回答

## 阶段

- [x] 阶段1: 回读项目经验与主线/支线历史上下文
- [x] 阶段2: 定位实验目录、评估脚本和 `NSPR` 定义
- [x] 阶段3: 提取或重算目标实验的 `NSPR`
- [x] 阶段4: 复核证据并整理交付结论

## 关键问题

1. 当前项目里是否存在真正的 `NSPR` 指标: 没有找到。静态代码只发现 `PSNR / SSIM / LPIPS`, 因此用户口中的 `NSPR` 需要按 `PSNR` 口径理解, 并在交付时明确说明。
2. 为什么不能直接照搬 `python -m ours.evaluation --eval_test`: 因为这条脚本的 test 循环按 `refine_start_idx/refine_end_idx` 取样, 当前配置是 `0..100`, 但真实 test split 只有 `41` 张, 口径不匹配。
3. 当前最可靠的动态评估口径是什么:
   - 口径A: 按真实 `test` split 的 `41` 张重算 `PSNR / SSIM / LPIPS`。
   - 口径B: 按当前实验 before/after 固定窗口对应的前 `100` 张 `train` 图重算 `PSNR / SSIM / LPIPS`。
   - 口径C: 按完整 `train` split 的 `283` 张重算 `PSNR / SSIM / LPIPS`。

## 做出的决定

- 决定4: 最终回复里明确写出“仓库里没有 `NSPR`, 本次按 `PSNR` 解释并重算”。
- 决定5: 主数字优先给出真实 `test` split 的 `PSNR`, 同时附上固定窗口 `train` 和全量 `train` 的对照, 避免口径误会。
- 决定6: 明确标注当前实验已经把 `test` split 纳入训练与 synthetic source pool, 所以 test 数值存在 benchmark 污染, 只能当作“按 test split 重算的观察值”, 不能当作干净 holdout。

## 状态

**目前已完成** - 已按项目 `Refiner.render(..., eval=True)` 的同口径逻辑重算 base/refined 指标, 并整理出可交付结论。
